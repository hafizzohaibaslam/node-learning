// common Js, every file is module (by default)
// Module _ Encapsulated code (only share minimum)

const names = require('./4-name');      // gets an object 
const sayHi = require('./3-module2');      // gets a function 
const data = require('./moduleAlternative');

require('./module3')



// console.log(name);  name get an object  

// console.log(sayHi);

// sayHi('something');
// sayHi(names.zohaib);
// sayHi(names.Aslam);


